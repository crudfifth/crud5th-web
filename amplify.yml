version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 22
        - npm ci
    build:
      commands:
        - npm run check
        - npm run build
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
